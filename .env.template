# 数据存储根目录 (支持相对路径，如 ./data)
DATA_ROOT=./data

# --- 以下为可选配置，默认基于 DATA_ROOT 自动生成 ---

# 虚拟环境基础路径 (默认: $DATA_ROOT/envs)
# ENVS_BASE_PATH=./data/envs

# UV 缓存目录 (默认: $DATA_ROOT/uv_cache)
# ⚠️ 注意：必须与 ENVS_BASE_PATH 在同一个物理分区以支持 Hardlink 优化
# UV_CACHE_DIR=./data/uv_cache

# 默认 Python 版本
DEFAULT_PYTHON=3.11

# 命令执行超时时间 (秒)
EXECUTION_TIMEOUT=30

# 闲置环境清理阈值 (小时)
CLEANUP_IDLE_HOURS=72

DATABASE_URL=postgresql://agentgit:agentgit_dev_password@localhost:54320/agentgit_testing